<%= simple_form_for @place, html: { class: 'form-horizontal' } do |f| %>
	<%= f.input :title %>
	<%= f.input :visited_by %>
	<%= f.input :address %>
	<%= f.button :submit, class: "btn btn-primary" %>
<% end %>